# Exemplo de configuração das variáveis de ambiente

# === GOOGLE OAUTH CONFIGURATION ===
# Para configurar o login com Google, você precisa:
# 1. Criar um projeto no Google Cloud Console (https://console.cloud.google.com/)
# 2. Ativar a Google+ API
# 3. Criar credenciais OAuth 2.0
# 4. Configurar as URLs de callback autorizadas

# ID público da sua aplicação Google
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# Segredo confidencial da sua aplicação Google (mantenha em segredo!)
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# URL de callback para onde o Google deve redirecionar após autenticação
# Para desenvolvimento local: http://localhost:3005/auth/google/callback
# Para produção: https://seu-dominio.com/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:3005/auth/google/callback

# === SUPABASE CONFIGURATION ===
# Estas são configuradas automaticamente quando você conecta ao Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# === OUTRAS CONFIGURAÇÕES ===
# URL da API (opcional, usa proxy se não definida)
VITE_API_URL=

# Ambiente (development/production)
NODE_ENV=development